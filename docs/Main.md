Title: Main entry

Source: [src/main.jsx](src/main.jsx)

Description:

- Application entry point. Renders `App` into the DOM root using React 18 `createRoot`.

Behavior:

- Imports global styles and mounts the app inside `document.getElementById('root')`.
